<!-- 客户端存储 -->
<view>
    <text class="title" wx:for="{{items}}">
        {{index}}. {{item}}
    </text>
    <input  placeholder="请输入" model:value="{{inputValue}}" />
    <button bindtap="addItem">添加</button>
</view>